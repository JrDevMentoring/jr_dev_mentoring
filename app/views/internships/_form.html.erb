<%= simple_form_for @internship do |f| %>
  <%= f.input :title %>
  <%= f.input :description %>
  <%= f.input :application_link, label: 'Link to application page' %>
  <%= f.button :submit, 'Post position' %>
<% end %>
